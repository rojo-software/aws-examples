{
  "name": "authorizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "publish": "rm -rf node_modules; npm install --omit=dev; zip -r authorizer.zip * -x test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.451.0"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^3.0.0",
    "jest": "^29.7.0"
  }
}
