{
  "name": "processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "publish": "rm -rf node_modules; npm install --omit=dev; zip -r lambda.zip * -x test/**"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.441.0",
    "@aws-sdk/lib-dynamodb": "^3.441.0",
    "tv4": "^1.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
